/**
 * Class that holds a utility methods for tests.
 */
export declare class TestUtils {
    /**
     * Method to set test timeout based on --testTimeout param passed into the process
     */
    static setTestTimeout(testClass: any, defaultTimeout?: number): void;
}
