import { IErrorReportingService } from './IErrorReportingService';
import { IGlimpseError } from './IGlimpseError';
import { ITelemetryService } from '../telemetry/TelemetryService';
export declare class TelemetryErrorReportingService implements IErrorReportingService {
    private telemetryService;
    private sentDependencies;
    constructor(telemetryService: ITelemetryService);
    reportError(error: IGlimpseError, ...params: any[]): void;
    private sendDependencies();
}
