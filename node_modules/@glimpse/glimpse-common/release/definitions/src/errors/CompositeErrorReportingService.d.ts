import { IErrorReportingService } from './IErrorReportingService';
import { IGlimpseError } from './IGlimpseError';
export declare class CompositeErrorReportingService implements IErrorReportingService {
    private errorServices;
    constructor(services: IErrorReportingService[]);
    reportError(error: IGlimpseError, ...params: any[]): void;
}
