import { IMessage } from './IMessage';
import { IMessagePublisher, IMessageStreamCallback, IMessageStreamSubscription } from './IMessagePublisher';
import { IMessageStorage } from '../storage/IMessageStorage';
export declare class MessagePublisher implements IMessagePublisher {
    private _messageStorage;
    private _subscriptions;
    constructor(messageStorage: IMessageStorage);
    publishMessages(messages: IMessage[]): void;
    streamMessages(contextId?: string, types?: string[], callback?: IMessageStreamCallback): IMessageStreamSubscription;
    private publishMessage(message);
}
