import { InMemoryMessageStorageContext } from './InMemoryMessageStorageContext';
import { IMessage } from '../messaging/IMessage';
import { IMessageStorage } from './IMessageStorage';
export declare class InMemoryMessageStorage implements IMessageStorage {
    private static MAX_CONTEXTS;
    private static REQUEST_CONTEXT_TYPE;
    private cache;
    constructor(maxContexts?: number);
    contexts: {
        [key: string]: InMemoryMessageStorageContext;
    };
    persist(message: IMessage): void;
}
