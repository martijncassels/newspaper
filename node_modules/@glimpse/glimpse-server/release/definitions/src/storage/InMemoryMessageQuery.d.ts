import { IMessage } from '../messaging/IMessage';
import { IMessageQuery } from './IMessageQuery';
import { IMessageStorage } from './IMessageStorage';
import { IRequestFilters } from './IRequestFilters';
export declare class InMemoryMessageQuery implements IMessageQuery {
    private static MAX_REQUESTS_PER_PAGE;
    private storage;
    constructor(storage: IMessageStorage);
    queryMessages(contextId?: string, types?: string[]): IMessage[];
    queryRequests(filters?: IRequestFilters, types?: string[]): IMessage[];
}
