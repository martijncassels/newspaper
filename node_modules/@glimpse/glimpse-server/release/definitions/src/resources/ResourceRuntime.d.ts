/// <reference types="express" />
import { IErrorReportingService } from '@glimpse/glimpse-common';
import { IResourceAuthorization } from './IResourceAuthorization';
import { IResourceManager } from './IResourceManager';
import * as express from 'express';
export declare class ResourceRuntime {
    private resourceAuthorization;
    private resourceManager;
    private errorReportingService;
    constructor(resourceAuthorization: IResourceAuthorization, resourceManager: IResourceManager, errorReportingService: IErrorReportingService);
    processRequest(req: express.Request, res: express.Response, next?: Function): void;
}
