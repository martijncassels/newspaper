/// <reference types="node" />
import * as http from 'http';
import { IResource } from './IResource';
import { IResourceManager } from './IResourceManager';
export declare class ResourceManager implements IResourceManager {
    private resourceTable;
    private getStartingSegment(path);
    readonly resources: IResource[];
    getUriTemplates(baseUri: string): {
        [key: string]: string;
    };
    register(resource: IResource): void;
    match(req: http.ServerRequest): {
        parameters: any;
        resource: IResource;
    };
}
