/// <reference types="express" />
import { IConfigSettings } from '@glimpse/glimpse-common';
import { IResource } from './IResource';
import * as express from 'express';
export declare class UriTemplate {
    static getBaseUri(configSettings: IConfigSettings, req: express.Request): string;
    static fromResource(baseUri: string, resource: IResource): string;
}
