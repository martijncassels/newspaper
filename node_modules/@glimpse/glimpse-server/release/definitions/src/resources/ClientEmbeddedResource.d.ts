import { IResource } from './IResource';
import { IServer } from './../IServer';
export declare class Resource implements IResource {
    private baseUrl;
    private baseUrlDirectory;
    private clientDirectory;
    uriTemplate: string;
    name: string;
    type: string;
    templateName: string;
    private configSettings;
    constructor(server: IServer, urlSuffix: string, contentLocationSuffix: any);
    invoke(req: any, res: any, next: any): any;
    private needsExperimentalFlag(baseName, parsedUrl);
    private needsPathName(baseName, parsedUrl);
    private needsBaseUrl(baseName, parsedUrl);
    private needsMetadatUri(baseName, parsedUrl);
}
