'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var Resource = (function () {
    function Resource(server) {
        this.name = 'message-ingress';
        this.templateName = 'messageIngressTemplate';
        this.type = 'agent';
        if (server) {
            this.init(server.providers.messagePublisher);
        }
    }
    Resource.prototype.init = function (messagePublisher) {
        this.messagePublisher = messagePublisher;
    };
    Resource.prototype.invoke = function (req, res) {
        // TODO: need to respond with processed message IDs (see #51)
        // TODO: need to validate posting (see #51)
        this.messagePublisher.publishMessages(req.body);
        res.sendStatus(202);
    };
    ;
    return Resource;
}());
exports.Resource = Resource;

//# sourceMappingURL=../../../maps/src/resources/MessageIngressResource.js.map
