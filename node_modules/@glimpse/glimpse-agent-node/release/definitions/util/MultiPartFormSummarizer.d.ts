/// <reference types="node" />
import { IPartSummary } from '../inspectors/IServerRequestInspector';
export interface IMultiPartFormSummarizer {
    addChunk(chunk: Buffer | string): any;
    getParts(): IPartSummary[];
}
export declare function createMultiPartFormSummarizer(contentTypeHeader: string): IMultiPartFormSummarizer;
/**
 * IMultiPartFormSummarizer that uses dicer parser for multi-part form summaries
 */
export declare class DicerMultiPartSummarizer implements IMultiPartFormSummarizer {
    private partSummaries;
    private dicer;
    private failed;
    constructor(boundary: string);
    addChunk(chunk: Buffer | string): void;
    getParts(): IPartSummary[];
}
/**
 * MultiPartFormSummarizer that uses custom parser for multi-part form summaries
 */
export declare class CustomParserMultiPartSummarizer implements IMultiPartFormSummarizer {
    private parser;
    constructor(boundary: string);
    addChunk(chunk: Buffer | string): void;
    getParts(): IPartSummary[];
    private convertRawHeaders(rawHeaders);
}
