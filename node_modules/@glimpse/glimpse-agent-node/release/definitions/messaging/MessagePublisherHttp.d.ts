import { IMessagePublisher } from './IMessagePublisher';
import { IResourceProvider } from '../configuration/IResourceProvider';
export declare class MessagePublisherHttp implements IMessagePublisher {
    private resourceProvider;
    private static messageIngressTemplate;
    constructor(resourceProvider: IResourceProvider);
    publishMessages(messages: any): void;
}
