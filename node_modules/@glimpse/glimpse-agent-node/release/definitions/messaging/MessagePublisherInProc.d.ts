import { IMessagePublisher } from './IMessagePublisher';
export declare class MessagePublisherInProc implements IMessagePublisher {
    private server;
    constructor(server: any);
    publishMessages(messages: any): void;
}
