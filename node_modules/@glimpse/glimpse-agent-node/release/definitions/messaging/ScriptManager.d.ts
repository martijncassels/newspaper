import { IContext, IContextManager } from './IContextManager';
import { IResourceProvider } from '../configuration/IResourceProvider';
import { IScriptManager } from './IScriptManager';
export declare class ScriptManager implements IScriptManager {
    private contextManager;
    private resourceProvider;
    private static hudScriptTemplate;
    private static clientScriptTemplate;
    private static contextTemplate;
    private static metadataTemplate;
    private static browserAgentScriptTemplate;
    private static messageIngressTemplate;
    private packageVersion;
    constructor(contextManager: IContextManager, resourceProvider: IResourceProvider);
    injectScript(htmlbody: any, payload: string): any;
    private getCurrentContextId(context);
    private wrapScripts(scripts);
    private hudScript(context, resources);
    private agentScript(context, resources);
    getScriptTagsForCurrentRequest(context: IContext): string;
}
