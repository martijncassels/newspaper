import { IProxy } from './IProxy';
import { IConfigSettings, IErrorReportingService } from '@glimpse/glimpse-common';
import { IAgent } from '../IAgent';
export declare abstract class ProxyBase implements IProxy {
    moduleName: string;
    abstract init(agent: IAgent, module: any, resolvedModulePath: string, errorReportingService?: IErrorReportingService): any;
    isEnabledForModule(moduleName: string, configSettings: IConfigSettings): boolean;
    /**
     *  returns true if the module should be loaded immediated after proxy initialization, false if not
     */
    readonly forceLoadModule: boolean;
}
