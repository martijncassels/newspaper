import { IAgent } from '../IAgent';
import { ProxyBase } from './ProxyBase';
export declare class BodyParserProxy extends ProxyBase {
    readonly moduleName: string;
    init(agent: IAgent, module: any): any;
    private static wrapParserFactory(parserFactory, name, displayName);
}
