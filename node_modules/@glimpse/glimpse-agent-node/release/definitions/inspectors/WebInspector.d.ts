/// <reference types="node" />
import { DateTimeValue } from '../configuration/DateTimeValue';
import { IAgent } from '../IAgent';
import { IServerRequestInspector, IPartSummary } from './IServerRequestInspector';
import * as http from 'http';
export declare class WebInspector implements IServerRequestInspector {
    private agent;
    private dateTime;
    private getCurrentTimeStamp();
    requestStart(req: http.IncomingMessage, res: http.ServerResponse, requestStartTime: any): void;
    private getRequestUrl(req);
    requestEnd(req: http.IncomingMessage, res: http.ServerResponse, content: Array<any>, size: number, requestStartTime: DateTimeValue, multiPartSummaries?: IPartSummary[]): void;
    responseStart(req: http.IncomingMessage, res: http.ServerResponse, responseStartTime: DateTimeValue): void;
    responseEnd(req: http.IncomingMessage, res: http.ServerResponse, content: Array<any>, size: number, responseStartTime: DateTimeValue): void;
    init(agent: IAgent): void;
}
