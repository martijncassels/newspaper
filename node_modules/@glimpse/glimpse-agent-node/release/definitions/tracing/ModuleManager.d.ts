import { IModuleManager } from './IModuleManager';
import { IModuleInstrumentor } from './IModuleInstrumentor';
import { IAgent } from '../IAgent';
import { IErrorReportingService } from '@glimpse/glimpse-common';
export declare class ModuleManager implements IModuleManager {
    private nonExperimentalModules;
    private agent;
    private moduleInstrumentors;
    private unsupportedVersions;
    private errorReportingService;
    private moduleModule;
    private originalRequire;
    init(agent: IAgent, moduleModule: any): void;
    /**
     * returns true if this is an experimental module, false otherwise
     */
    private isExperimentalModeModule(moduleId);
    destroy(): void;
    addModuleInstrumentor(instrumentor: IModuleInstrumentor): void;
    setErrorReportingService(service: IErrorReportingService): void;
}
