import { IModuleInfo } from '../IModuleInfo';
export declare class HttpClientProxy {
    private proxiedModules;
    private publishRequestCreatedEvent(data);
    private publishRequestDataSentEvent(data);
    private publishRequestEndEvent(data);
    private publishRequestErrorEvent(data);
    private publishResponseReceivedEvent(data);
    private publishResponseDataReceivedEvent(data);
    private publishResponseEndEvent(data);
    private publishResponseErrorEvent(data);
    init(moduleInfo: IModuleInfo): void;
}
