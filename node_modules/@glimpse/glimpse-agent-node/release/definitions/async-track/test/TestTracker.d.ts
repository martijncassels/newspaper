/// <reference types="mocha" />
import { IAsyncTrackEvents } from '../async-track';
export declare class TestTracker implements IAsyncTrackEvents {
    private events;
    private state;
    static STATE_KEY: string;
    register(): void;
    private deRegister();
    /**
     * Wrap a mocha done handler to de-register this async-track listener when test is completed.
     */
    wrapDone(done: MochaDone): (error?: Error) => void;
    onAsyncTransition(parentId: any, id: any): Object;
    private getInvocationData(id);
    onBeforeInvocation(id: any): void;
    onAfterInvocation(id: any): void;
    onError(msg: string): void;
    onWarning(msg: string): void;
    setState(state: string): void;
    validateInvocation(id: any, expectedNamePath: string[] | string, expectedStateString: any): void;
}
