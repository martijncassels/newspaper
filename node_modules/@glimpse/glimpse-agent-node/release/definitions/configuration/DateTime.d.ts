import { DateTimeValue } from './DateTimeValue';
import { IDateTime } from './IDateTime';
import moment = require('moment');
export interface INowFactory {
    time: () => moment.Moment;
    offset: () => number[];
}
export declare class DateTime implements IDateTime {
    private nowFactory;
    constructor(nowFactory?: INowFactory);
    readonly now: DateTimeValue;
}
