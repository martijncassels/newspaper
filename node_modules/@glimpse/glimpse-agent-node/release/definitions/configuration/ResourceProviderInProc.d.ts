import { IContextManager } from '../messaging/IContextManager';
import { IResourceProvider } from './IResourceProvider';
export declare class ResourceProviderInProc implements IResourceProvider {
    private contextManager;
    private server;
    constructor(contextManager: IContextManager, server: any);
    getResourceDefinitions(): {
        [key: string]: string;
    };
}
