import { IContextManager } from '../messaging/IContextManager';
import { IDeferredInitializationManager } from '../IDeferredInitializationManager';
import { IResourceProvider } from './IResourceProvider';
export interface IResourceProviderOptions {
    metadataUri?: string;
    server?: any;
}
export declare class ResourceProvider implements IResourceProvider {
    private provider;
    getResourceDefinitions(): {
        [key: string]: string;
    };
    init(contextManager: IContextManager, deferredInitializationManager: IDeferredInitializationManager, options: IResourceProviderOptions): void;
}
