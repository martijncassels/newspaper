import moment = require('moment');
export declare class DateTimeValue {
    private value;
    private offset;
    constructor(value: moment.Moment, offset: number);
    static fromUnixMillisecondTimestamp(unixTimeStamp: number, hrTime: number[]): DateTimeValue;
    static fromMomentAndHRTime(m: moment.Moment, hrTime: number[]): DateTimeValue;
    diff(value: DateTimeValue): number;
    getUnixTimestamp(): number;
    getMoment(): moment.Moment;
    getOffset(): number;
    format(includeUTCOffset?: boolean): string;
    getUTCOffset(includeColon?: boolean): any;
    private static toTwoDigits(value);
    private static toThreeDigits(value);
}
