import { IDeferredInitializationManager } from '../IDeferredInitializationManager';
import { IResourceProvider } from './IResourceProvider';
export declare class ResourceProviderHttp implements IResourceProvider {
    private metadataUri;
    private deferredInitializationManager;
    private static EXPORT_CONFIG_RESOURCE_NAME;
    private resources;
    constructor(metadataUri: string, deferredInitializationManager: IDeferredInitializationManager);
    getResourceDefinitions(): {
        [key: string]: string;
    };
}
