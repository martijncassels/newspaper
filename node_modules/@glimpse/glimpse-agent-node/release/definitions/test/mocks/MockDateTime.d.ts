import { IDateTime } from '../../configuration/IDateTime';
import { DateTimeValue } from '../../configuration/DateTimeValue';
export declare class MockDateTime implements IDateTime {
    private nows;
    private index;
    constructor(nows?: DateTimeValue[]);
    readonly now: DateTimeValue;
}
