import { IAgent, IAgentProviders } from '../../IAgent';
import { IAgentBroker } from '../../messaging/IAgentBroker';
import { IMessage } from '../../messaging/IMessage';
export interface ISpy {
    (msg: IMessage<any>): void;
}
export declare class MockAgent implements IAgent {
    private publishMessages;
    broker: IAgentBroker;
    private dateTimeService;
    providers: IAgentProviders;
    constructor();
    init(publishMessages?: boolean): void;
    private spies;
    addMessageSpy(spy: ISpy): void;
    removeAllMessageSpies(): void;
    private createAndSendMessage(data, types, indices, context);
    private sendMessage(message);
}
