"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var NodeVersionHelper = (function () {
    function NodeVersionHelper() {
    }
    NodeVersionHelper.fillInVersionNumbers = function () {
        var v = process.version.substring(1, process.version.length);
        var splits = v.split('.');
        NodeVersionHelper.major = parseInt(splits[0], 10);
        NodeVersionHelper.minor = parseInt(splits[1], 10);
        NodeVersionHelper.revision = parseInt(splits[2], 10);
    };
    NodeVersionHelper.getMajor = function () {
        if (!NodeVersionHelper.major) {
            NodeVersionHelper.fillInVersionNumbers();
        }
        return NodeVersionHelper.major;
    };
    NodeVersionHelper.getMinor = function () {
        if (!NodeVersionHelper.minor) {
            NodeVersionHelper.fillInVersionNumbers();
        }
        return NodeVersionHelper.minor;
    };
    NodeVersionHelper.getRevision = function () {
        if (!NodeVersionHelper.revision) {
            NodeVersionHelper.fillInVersionNumbers();
        }
        return NodeVersionHelper.revision;
    };
    return NodeVersionHelper;
}());
exports.NodeVersionHelper = NodeVersionHelper;

//# sourceMappingURL=../../maps/util/NodeVersionHelper.js.map
